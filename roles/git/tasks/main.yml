---
- name: install git
  yum: name={{ item }} state=installed
  with_items:
    - git

  # pass is generated by python -c 'import crypt; print crypt.crypt("git", "salt")'
- name: create git user
  user: name=git password=saiVaAfvSmkJM home={{ user_home }}

- name: chmod 0775 {{ user_home }}
  file: path={{ user_home }} mode=0775